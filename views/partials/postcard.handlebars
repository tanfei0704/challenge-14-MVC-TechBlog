<div class="card">
    <div class="card-header">
       <h4>{{title}}</h4>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{content}}</p>
    <div class="card-footer">
        <a href="/posts/{{id}}" class="link">Click to see details !</a>
    </div>      
</div>